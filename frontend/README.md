# Frontend

Implement the UI described in `SmartTag_fw/docs/04-frontend.md` and contracts in `SmartTag_fw/docs/01-contracts.md`.

Point the dev server at the backend API/WebSocket URL (see `SmartTag_fw/docs/06-local-lan-deploy.md` for LAN access).
