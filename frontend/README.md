# Frontend

Implement the UI described in [`SmartTag_fw/docs/04-frontend.md`](../../SmartTag_fw/docs/04-frontend.md) and contracts in [`SmartTag_fw/docs/contracts.md`](../../SmartTag_fw/docs/contracts.md). Plan index: [`SmartTag_fw/docs/plan.md`](../../SmartTag_fw/docs/plan.md).

Point the dev server at the backend API/WebSocket URL (see `SmartTag_fw/docs/06-local-lan-deploy.md` for LAN access).

**Быстрый набросок (без сборки):** статика в `demo/` — отдаётся тем же процессом, что `uvicorn demo_server:app` из `../backend/README.md` (порт 8787).
