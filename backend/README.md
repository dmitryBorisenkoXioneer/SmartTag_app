# Backend

Implement the service described in `SmartTag_fw/docs/03-backend.md` and `SmartTag_fw/docs/01-contracts.md` (MQTT topics, DB feature rows, train job, WebSocket for UI).

Connect to Postgres/Timescale and Mosquitto from `SmartTag_fw/deploy/docker-compose.yml` when developing locally.
